# Fuel Station — แผนพัฒนาเว็บสำหรับ Sol

## เป้าหมายและขอบเขต

เจ้าของปั๊มต้องรับลูกค้าต่อเนื่อง กรอกยอดขายได้เร็ว แม้เน็ตช้าหรือขาดชั่วคราว โดยเงิน สต๊อก และมิเตอร์ตรวจสอบย้อนหลังได้ หน้าจอภาษาไทย เรียบง่าย อ่านกลางวันได้ ใช้เว็บเดียวบนคอมและเบราว์เซอร์ iOS/Android ก่อน ไม่ต้องสร้าง native app ในรอบนี้

นี่เป็นเอกสารวางแผน ยังไม่ได้ทำระบบคิวหรือเปลี่ยนฐานข้อมูล ผู้ใช้ตั้งใจให้ Sol เป็นผู้ implement ต่อ อ่าน AGENTS.md และเอกสาร Next.js ใน node_modules/next/dist/docs ก่อนเขียนโค้ด

## สภาพปัจจุบันที่ตรวจพบ

- Stack: Next.js 16.2.6, React 19, Tailwind 4, Prisma Client 5.22, PostgreSQL ตาม schema ตรวจเวอร์ชันที่ติดตั้งจริงอีกครั้ง
- app/api/sales/route.ts สร้าง Sale และลด FuelStock ใน transaction เดียว เป็นฐานที่ควรเก็บ
- app/api/purchases/route.ts เพิ่มสต๊อกพร้อมบันทึกรับเข้า
- app/api/meter-periods แยกจาก Sale แต่ MeterPeriod ไม่มีหัวจ่าย ไม่มีเวลาเปิด/ปิดรอบแยก ไม่มี relation ไปยังรายการขาย
- app/api/fuel-stock/compare ใช้มิเตอร์ที่ปิดแล้วแทน Sale ทั้งวันของน้ำมันชนิดนั้น เสี่ยงตกหล่นเมื่อมีหลายรอบต่อวัน สูตรคล้ายกันอยู่ใน sales/summary ต้องแก้ร่วมกัน
- stock-checks POST เขียนทับ FuelStock ปัจจุบัน แม้กรอกวันที่ย้อนหลัง อ่าน systemLiters ก่อนเข้า transaction ด้วย
- Sale ไม่มี clientRequestId สำหรับป้องกันส่งซ้ำ API ตรวจตัวเลขยังไม่ครบ
- PIN ใน components/PinModal.tsx ตรวจ localStorage เป็นเพียงการซ่อนหน้าจอ ไม่ใช่สิทธิ์ฝั่งเซิร์ฟเวอร์
- Dashboard รอบก่อนปรับ responsive, error/retry, AbortController และกันกดซ้ำระหว่างรอ แต่ยังมี optimistic update และ refreshDay ที่ต้องทบทวนเพื่อให้แสดงยอดสอดคล้องกัน
- ฐานข้อมูล local เคยตอบ tenant/user not found และ API 500 ไม่ทราบว่า production เป็นเหมือนกันหรือไม่ ต้องตรวจใหม่ ไม่สรุปว่าเกิดจากเน็ตอย่างเดียว
- Worktree มีงานอื่นของผู้ใช้และการแก้ Dashboard เดิมอยู่ ห้าม reset หรือเขียนทับงานที่ไม่เกี่ยวข้อง

## ทิศทาง UX/UI

ใช้พื้นขาว/เทาอ่อน ตัวหนังสือเข้ม สีหลักน้ำเงินเข้ม สถานะใช้ทั้งข้อความและสี ฟอนต์ไทยอ่านง่าย ตัวเลข tabular ไม่เพิ่ม animation หรือกราฟหนักในหน้าขาย ปุ่มอย่างน้อย 44px และ contrast 4.5:1

เมนูหลัก 5 หน้า: ขาย / ภาพรวม / สต๊อก / มิเตอร์ / เพิ่มเติม บนคอมใช้ sidebar บนมือถือใช้ bottom navigation พร้อม safe area ไม่ทับปุ่มบันทึก รองรับความกว้าง 320, 390, 768, 1024, 1440px

### หน้าขาย: เส้นทางหลักของพนักงาน

แถบบนแสดงชื่อพนักงาน หัวจ่าย รอบที่เปิด และสถานะส่งข้อมูล; เลือกชนิดน้ำมันตามหัวจ่าย; ยอดยอดนิยม + ช่องกรอกเงินเองแบบ numeric; เงินสด/โอน/เครดิต; เครดิตต้องระบุลูกค้า; แสดงราคาและลิตร; ปุ่มบันทึกอยู่ตำแหน่งเดิม; รายการล่าสุดด้านล่างพร้อมสถานะรายรายการ

จำหัวจ่ายและวิธีชำระล่าสุด แต่ไม่เดาราคาย้อนหลัง เมื่อเก็บลงเครื่องสำเร็จให้ล้างยอดเงินและพร้อมรับลูกค้ารายถัดไปทันที ข้อความต้องเป็น “เก็บในเครื่องแล้ว · รอส่ง” จนกว่า server ยืนยัน ห้ามใช้ “บันทึกแล้ว” แทนการเขียนฐานข้อมูลที่ยังไม่เกิด

### หน้าภาพรวม

แสดงยอดตามวันที่เลือก เงินสด/โอน/เครดิต ลิตรขาย รายการล่าสุด และสิ่งที่ต้องจัดการก่อน เช่น คิวค้าง/รอบยังไม่ปิด/สต๊อกต่ำ รายงานยอดยืนยันกับรายการรอส่งบนเครื่องนี้ต้องแยกกัน ไม่รวมแล้วทำให้เข้าใจว่าเป็นยอดยืนยันทั้งหมด

### หน้าสต๊อก

แยกชัดเจน: สต๊อกตามบัญชีขาย / ค่าประมาณตามมิเตอร์ / ผลวัดจริงล่าสุด พร้อมวันเวลาและแหล่งที่มา สต๊อกของน้ำมันร่วมถังต้องรวมได้ มีรับน้ำมันเข้า ประวัติการเคลื่อนไหว และรายการปรับยอดพร้อมเหตุผล ไม่แสดง progress bar เป็นเปอร์เซ็นต์ความจุหากยังไม่มีความจุถังจริง

### หน้ามิเตอร์

แสดงรอบเปิดต่อหัวจ่าย เปิดรอบด้วยเลขต้น ปิดรอบด้วยเลขปลาย เปรียบเทียบลิตรจากมิเตอร์กับรายการขายในรอบเดียวกัน แสดงส่วนต่างให้เจ้าของตรวจ ไม่มีการสร้างยอดขายเพิ่มอัตโนมัติเพื่อบังคับให้ตรง

## กติกาข้อมูลที่ต้องรักษา

1. ยอดขายหนึ่งรายการหักสต๊อกเพียงครั้งเดียว แม้ retry พร้อมกันหลายคำขอ หลายแท็บ หรือ server บันทึกแล้วแต่ response หาย
2. การปิดมิเตอร์เป็นการตรวจเทียบ ไม่หักสต๊อกซ้ำ ส่วนต่างที่ต้องปรับจริงต้องมีรายการปรับปรุงแยกพร้อมเหตุผลและผู้อนุมัติ
3. บันทึกเวลาขายจริงแยกจากเวลาที่ server รับ ใช้เวลาไทยสำหรับวันรายงาน เก็บ timestamp เป็น UTC พร้อม timezone rule ชัดเจน ห้ามใช้วันที่ซิงก์เป็นวันที่ขาย
4. ราคาในรายการเป็น snapshot ณ ขาย ไม่คำนวณย้อนหลังด้วยราคาปัจจุบัน เปลี่ยนราคากลางรอบต้องแบ่งช่วงหรือเปิดรอบใหม่ หากเทียบรายได้จากมิเตอร์ต้องรู้ราคาของช่วงนั้น
5. ใช้ Decimal/หน่วยคงที่สำหรับเงินและลิตร ระบุ precision และวิธีปัด ตรวจผล migration กับข้อมูล Float เดิมก่อน
6. ห้ามแก้ย้อนหลังด้วยการ overwrite ยอดปัจจุบัน เลือกบันทึก correction และคำนวณตามเวลาที่มีผล พร้อมทดสอบรายการก่อน/หลังจุดวัดจริง
7. ยอดรอส่งเป็นข้อมูลเฉพาะอุปกรณ์ เครื่องอื่นยังไม่เห็น ต้องบอกผู้ใช้ตรง ๆ

## โครงสร้างที่เสนอ

- Pump/Nozzle: รหัสหัวจ่าย ชนิดน้ำมัน และถังที่ใช้ (ยืนยันการติดตั้งจริงก่อน map ข้อมูลเก่า)
- MeterPeriod: nozzleId, openedAt, closedAt, meterStart, meterEnd, status; เปิดได้ไม่เกินหนึ่งรอบต่อหัวจ่าย บังคับฝั่งฐานข้อมูล/transaction
- Sale: เพิ่ม clientRequestId แบบ unique ในขอบเขตสถานี, meterPeriodId, occurredAt/เวลาขาย, server receivedAt, actor และ price snapshot
- StockMovement: ประวัติ +รับเข้า −ขาย ±ปรับปรุง/กลับรายการ ผูก sourceId และเหตุผล; unique source ป้องกัน movement ซ้ำ
- FuelStock เป็นยอดรวมที่เก็บไว้เพื่ออ่านเร็วและตรวจสร้างใหม่จาก ledger ได้ ห้ามมีหลายสูตรเป็นคนละแหล่งความจริง
- ไม่ย้ายข้อมูลทั้งหมดทีเดียว เริ่มด้วย schema เพิ่มเติมและ compatibility path สำหรับรายการเก่า อย่าเดาว่า pumpNo ที่สะกดต่างกันเป็นหัวจ่ายเดียวกัน

## คิวขายที่ทนต่อเน็ตช้า

ใช้ IndexedDB เก็บรายการให้สำเร็จก่อน reset ฟอร์ม สถานะ queued → sending → synced หรือ needs-review เขียนไม่ได้/พื้นที่เต็มต้องแจ้งและคงฟอร์ม ห้ามรายงานว่าสำเร็จ

สร้าง UUID ต่อรายการ เก็บ payload และ request ID เดิมเมื่อ retry; server ใช้ unique constraint + transaction สร้าง Sale และ StockMovement พร้อมอัปเดต stock; request ID เดิม payload เดิมคืนผลเดิม; payload ต่างคืน conflict ไม่รับซ้ำ

ส่งคิวเมื่อแอปเปิด เมื่อกลับ online และเมื่อกดส่งใหม่ ใช้ backoff กับ network/5xx; 4xx ที่ต้องแก้ข้อมูลหยุด retry และแสดงปัญหา; 401 ให้ login แล้วส่งต่อโดยคงคิว ผูกคิวกับสถานี/บัญชี ป้องกันส่งคิวของผู้ใช้เก่าหลังสลับบัญชี

ใช้ single sender ต่อแท็บ/เครื่องเท่าที่ทำได้ แต่ความถูกต้องต้องอยู่ที่ server เพราะ lock ฝั่ง browser ไม่พอ ไม่พึ่ง Background Sync เป็นเงื่อนไขหลักบน iOS และไม่สัญญาว่าแอปปิดแล้วจะส่งเองเสมอ

การปิดรอบ: เครื่องที่ปิดต้องส่งคิวของตนก่อน รอบปิดแล้วเจอรายการมาช้าจากอีกเครื่องให้เข้า needs-review มีขั้นตอนรับเข้ารอบเดิมโดยเจ้าของและคำนวณเทียบใหม่ ห้ามทิ้งหรือย้ายไปรอบปัจจุบันอัตโนมัติ

## งานเรียงลำดับให้ Sol

แต่ละงานต้องมี commit/diff ที่ทบทวนได้ ใช้ tests กับกติกายอดและ concurrency; ทุกช่วงตรวจ TypeScript, lint เฉพาะไฟล์ที่แตะ และ build ตามสภาพแวดล้อม ห้ามอ้างผ่านหากยังต่อฐานข้อมูลไม่ได้

| งาน | ทำอะไร / ไฟล์หลัก | เกณฑ์ตรวจรับและวิธีตรวจ | ขึ้นกับ |
|---|---|---|---|
| 1 | ตรวจ DB/connection ใน lib/prisma.ts และ deployment config | GET อ่านข้อมูลได้ บันทึก baseline API p50/p95 แยก cold/warm; ไม่พิมพ์ secret; ถ้ายังติดให้รายงานเหตุและทำ UI ด้วย fixture ที่ระบุชัด | ไม่มี |
| 2 | เพิ่ม auth/บทบาทฝั่ง API ใช้กลไกที่เหมาะกับระบบเดิม | ผู้ไม่ login อ่าน/เขียนไม่ได้ พนักงานดูข้อมูลตามหน้าที่ เจ้าของปรับยอดได้; ทดสอบเรียก API ตรง ไม่ใช่ซ่อนปุ่ม | 1 |
| 3 | เพิ่ม request ID + validation ใน schema, sales POST และ service ขาย | ส่งซ้ำ/พร้อมกัน 10 ครั้งเหลือ Sale เดียวและหักสต๊อกครั้งเดียว; เงินติดลบ/NaN/Infinity/payment ผิดถูกปฏิเสธ; ทดสอบ DB จริงแยก | 2 |
| 4 | เพิ่ม IndexedDB queue และแถบสถานะในหน้าขาย | offline บันทึก 20 รายการ reload แล้วครบ; local write ล้มเหลวฟอร์มไม่หาย; reconnect ส่งครบไม่ซ้ำ; ทดสอบ timeout หลัง commit | 3 |
| 5 | ทำหน้าขายใหม่ใน app/quick หรือเลือก route เดียวเป็นหลัก | ขายซ้ำได้ต่อเนื่องโดยไม่รอ network; keyboard/มือถือใช้งานได้ มีกรอกยอดเอง/เครดิต/หัวจ่ายชัดเจน | 4 |
| 6 | เพิ่ม nozzle + meterPeriod relation และ migration แบบไม่เดาข้อมูล | หลายหัวจ่ายและหลายกะไม่ปน เปิดรอบซ้อนพร้อมกันถูกปฏิเสธ; ข้อมูลเก่าดูได้และมีรายการต้อง map | 3 |
| 7 | แก้เปิด/ปิดมิเตอร์และ compare รวมสูตร summary | เช้า 100L + บ่าย 50L ไม่หาย; ข้ามเที่ยงคืน/เปลี่ยนราคา/late sync ถูกกติกา; ห้ามหัก stock ซ้ำ | 4,6 |
| 8 | เพิ่ม ledger สำหรับ sale/รับเข้า จาก service กลาง | ledger สอดคล้อง stock; mutation ใดล้มเหลว rollback ทั้งชุด; ตรวจข้อมูลเดิมบนสำเนาก่อน migration | 3,6 |
| 9 | แก้ตรวจวัด/กลับรายการ/ย้อนหลังให้ใช้ ledger | รับเข้า 1000 ขาย 100 เหลือ 900; กลับขายคืน 100 เพียงครั้ง; correction ย้อนหลังไม่ทับธุรกรรมใหม่ | 7,8 |
| 10 | ทำ app shell + Dashboard/Stock/Meter UI ให้เป็นระบบเดียว | ทดสอบ 320–1440px, keyboard, focus, contrast, error/empty/loading และบัญชีพนักงาน/เจ้าของ | 5,7,9 |
| 11 | ปรับ query และอัปเดตหน้าจอเฉพาะข้อมูลที่เปลี่ยน | pagination รายการเก่า, index จาก query จริง, ไม่มี scan ประวัติทั้งหมดทุกครั้งที่ขาย; วัดก่อน/หลังด้วยข้อมูลชุดเดียวกัน | 9,10 |
| 12 | Preview deployment และตรวจรับด้วยข้อมูลทดสอบ | ผ่านขาย→รับเข้า→ปิดมิเตอร์→วัดจริง→รายงาน; มี screenshot และผลทดสอบ; production backup/rollback พร้อมก่อนปล่อยจริง | ทั้งหมด |

Checkpoint หลัง 3: เขียนยอดแบบไม่ซ้ำได้; หลัง 5: รับลูกค้าต่อเนื่องได้; หลัง 9: ตรวจยอดและย้อนประวัติได้; หลัง 12: พร้อมตรวจรับก่อน production งานใหญ่ให้แบ่งเป็น schema/service/UI migration ทีละชุด ไม่ทำทุกหน้ารวดเดียว

## เป้าหมายความเร็ว (ยังไม่ใช่ผลวัด)

- กดบันทึกถึงเก็บคิวในเครื่องและพร้อมรายการถัดไป: p95 ไม่เกิน 200ms บนอุปกรณ์ทดสอบที่ระบุ
- หน้า dashboard ใช้ข้อมูลเดิมขณะ refresh มีเวลาอัปเดตล่าสุด ไม่ทำหน้าขาว ไม่แสดง 0 แทนข้อผิดพลาด
- API warm เป้าหมาย p95 ไม่เกิน 500ms ในภูมิภาคใช้งานจริง วัด cold แยก; ไม่เพิ่ม caching ยอดเงินข้ามผู้ใช้เพื่อทำคะแนนให้ดี
- ทดสอบยอดประวัติ 10,000/100,000 รายการด้วยข้อมูลจำลองบนฐานทดสอบ วัด payload, query duration, request count, เวลาเปิดหน้า และเวลารับรายการใหม่ ไม่เอาเวลา compile dev มาเทียบ production

## สิ่งที่ต้องยืนยันจากเจ้าของก่อน migration จริง

จำนวนถัง/หัวจ่ายและการใช้ถังร่วมกัน; รอบต่อวัน/เวลาปิดข้ามวัน; จำนวนมือถือที่บันทึกพร้อมกัน; วิธีเปลี่ยนราคา; ใครอนุมัติปรับยอด/ขายย้อนหลัง ให้ทำ UI และทดสอบด้วย fixture ที่ระบุสมมติฐานก่อนได้ แต่ห้ามเดาการจับคู่ข้อมูลจริง

## Hugging Face และตัวอย่าง

ค้นวันที่ 7 กันยายน 2026 พบ DeepSite เป็นเครื่องมือสร้างและออกแบบเว็บด้วย AI รองรับหลายหน้า ใช้อ้างอิงหน้าตาหรือทำ mockup ได้ ไม่ใช่หลักฐานว่ามีระบบ POS/บัญชีน้ำมันที่พร้อมใช้: https://huggingface.co/spaces/enzostvs/deepsite (redirect https://deepsite.hf.co/)

พบ fuel-price-predictor-france ซึ่งเป็นเดโมคาดการณ์ราคาน้ำมัน ไม่ใช่ระบบขาย–สต๊อก–มิเตอร์: https://huggingface.co/spaces/QuentinVincent/fuel-price-predictor-france/blob/main/README.md

จากการค้นครั้งนี้ยังไม่พบ template ครบวงจรที่ตรวจยืนยันได้ ไม่ได้หมายความว่าไม่มี คง Next.js/ฐานข้อมูลเดิมแล้วให้ Sol ทำตาม flow นี้จะลดภาระย้ายระบบ ไม่ส่งข้อมูลลูกค้าหรือ secrets ไปใช้ใน public demo

## Prompt ส่งต่อ Sol

อ่าน PLAN-FOR-SOL.md และ AGENTS.md ใน C:/Users/USER/Documents/fuel-station ตรวจ working tree และการเปลี่ยนแปลงเดิมก่อน เริ่มงาน 1–3 แล้วทำตาม dependency ทีละ vertical slice พัฒนาหน้าขายที่บันทึกลง IndexedDB และส่งแบบ idempotent; เชื่อมรายการขายกับหัวจ่ายและรอบมิเตอร์; ใช้ ledger ที่ตรวจสอบย้อนหลังได้; ทำ UI ภาษาไทยเรียบง่าย responsive ตามแผน ใช้ข้อมูลจำลองแยกเมื่อฐานข้อมูลจริงยังติด ห้ามอ้างว่าทดสอบจริงแล้ว ห้าม seed/reset/migrate ฐาน production โดยไม่ตรวจเป้าหมายและสำรองก่อน ส่งหลักฐานทดสอบ ผลวัด และ preview ที่ผู้ใช้เปิดดูได้ ระบุเรื่องที่ยังไม่เสร็จทุกครั้ง ไม่ deploy production อัตโนมัติ
